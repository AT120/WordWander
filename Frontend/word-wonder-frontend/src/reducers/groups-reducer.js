const initialState = {
    groups: []
}

const groupsReducer = (state = initialState, action) => {
    let newState = { ...state }
    switch (action.type) {
        default:
            return newState
    }
}